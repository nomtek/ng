name: ng_example
description: Minimal demonstration of NG libbrary
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  ng:
    path: ../

flutter:
  uses-material-design: true
